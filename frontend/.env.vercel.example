# =============================================================================
# Vercel — Frontend Environment Variables
# Copy these into your Vercel project's Settings → Environment Variables.
# NEXT_PUBLIC_* variables are embedded at build time and visible in the browser.
# =============================================================================

# ── API connection to Railway backend ────────────────────────────────────────
NEXT_PUBLIC_API_URL=https://your-railway-app.railway.app/api
NEXT_PUBLIC_MEDIA_HOST=your-railway-app.railway.app

# ── Site URL (used for absolute links, OG tags, etc.) ────────────────────────
NEXT_PUBLIC_SITE_URL=https://your-vercel-app.vercel.app

# ── Stripe (publishable key is safe to expose) ───────────────────────────────
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_...

# ── Stripe server-side keys (keep secret — NOT prefixed with NEXT_PUBLIC_) ───
STRIPE_SECRET_KEY=sk_live_...
STRIPE_WEBHOOK_SECRET=whsec_...

# ── Build configuration ───────────────────────────────────────────────────────
# Do NOT set NEXT_OUTPUT_MODE here — Vercel handles Next.js natively.
# Only set NEXT_OUTPUT_MODE=standalone for Docker / Railway frontend builds.
NEXT_TELEMETRY_DISABLED=1
